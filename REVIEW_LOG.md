# TOEIC 互動自學教材｜完整檢查紀錄

版本：v1.1（2026-07-25）

## 顯示與操作修正

- 修正 12 個教材章節、共 150 處換行標記被顯示成 `<br>` 文字的問題。
- 商務例句的英文與中文翻譯現在會正確分行。
- 「今日進度」更名為「教材進度」，避免把整體完成率誤認為單日進度。
- 導讀章與檢查清單若沒有獨立題目，按鈕會明確顯示「本章沒有獨立題目」，不再點擊後才出現錯誤提示。
- 中文搜尋現在可輸入單一中文字，例如「費」也能搜尋。
- 題目進度條從第一題就會顯示實際進度，不再從 0% 開始。
- 深色／淺色模式按鈕的提示文字會跟著目前模式更新。
- 章節卡片補上鍵盤操作與焦點提示。
- 手機版完成水平溢位與功能檢查；寬表格可在表格區域左右滑動。

## 學習紀錄修正

- 學習日期改用使用者所在地的本地日期，不再使用 UTC 日期。台灣早上 8 點以前學習時，不會被錯記到前一天。
- 連續學習天數改用本地日期計算。
- 載入舊版瀏覽器紀錄時，會自動移除不存在的章節、題目與單字卡 ID。
- 異常的答題紀錄會被校正，例如答對次數不會大於作答次數。
- 無法使用瀏覽器儲存空間時，網站仍可開啟，不會因 localStorage 錯誤而整頁停止。

## 教材內容修正

- 重新區分「狀態動詞、可持續動作、瞬間／界限動詞」，移除把 `know` 當成動作動詞的矛盾內容。
- 釐清過去完成進行式只延續到「過去參照點」，不代表持續到現在。
- 修正限定／非限定關係子句的過度推論：逗號不能單獨證明某人只有一個兒子或只有一隻貓。
- 移除不正確的 V-ing 速記內容；補充 `ensure`、`regret`、`continue`、`forbid` 的正確限制。
- 修正 `I forgot calling`、`Regret to tell you` 等不自然例句。
- 修正 `continue to V / V-ing` 被過度簡化為「中斷／不中斷」的說法。
- 修正 `commit（犯病）` 為 `commit（犯下罪行）`，並移除不屬於 `-mit` 字根的 `vomit`。
- 修正 `instruction` 作為「說明書」的用法，改為 `instructions（說明／操作步驟）`。
- 修正關係子句表格中的「關頭」錯字及非限定子句說明。
- 修正 `主動詞一致` 為 `主詞動詞一致`。
- 修正 `attached in the email` 為 `attached to the email`。

## 題庫檢查

- 85 題皆完成欄位、答案索引、章節引用與重複 ID 檢查。
- 改寫 19 題的題幹、選項或解析，移除不自然選項與可能造成雙答案的設計。
- 主要修正包含時刻表時態、時間子句、被動語態、冠詞、動名詞、分詞構句、假設語氣、會議紀錄及 rise/raise/arise/arouse。

## 測試結果

- 22 個章節逐一開啟測試：通過。
- 150 個換行標記渲染測試：通過。
- 混合練習、即時解析、錯題寫入：通過。
- 單字卡翻面與熟悉標記：通過。
- 單一中文字搜尋：通過。
- 深色模式：通過。
- 舊版／異常 localStorage 資料清理：通過。
- 390px 手機版面與水平溢位檢查：通過。
- JavaScript 語法、瀏覽器 console error、page error：通過，未發現錯誤。
## UX 快捷入口更新

- 將首頁右側兩張外觀像按鈕、實際不可點擊的提示卡改成語意化 `<button>`。
- 左側按鈕明確標示「開始學習」，直接前往 Part 5 詞性秒判。
- 右側按鈕明確標示「立即作答」，直接開始 10 題混合練習。
- 新增 hover、active、focus-visible、箭頭回饋與 44px 以上觸控尺寸。
- 移除互動區塊的 `aria-hidden`，補上可供螢幕閱讀器辨識的標籤。

